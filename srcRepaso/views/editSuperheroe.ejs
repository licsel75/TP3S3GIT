<%- include('partials/header') %>

<h1><%= titulo %></h1>

<% if (errores) { %>
    <div class="alert error">
        <% errores.forEach(error => { %>
            <p>❌ <%= error.msg %></p>
        <% }); %>
    </div>
<% } %>

<form action="/dashboard/editar/<%= heroe._id %>?_method=PUT"
      method="POST"
      class="hero-form">

    <div class="form-group">
        <label>Nombre del Superhéroe *</label>
        <input type="text" name="nombreSuperHeroe" value="<%= heroe.nombreSuperHeroe %>" required>
    </div>
    
    <div class="form-group">
        <label>Nombre Real *</label>
        <input type="text" name="nombreReal" value="<%= heroe.nombreReal %>" required>
    </div>
    
    <div class="form-group">
        <label>Edad *</label>
        <input type="number" name="edad" value="<%= heroe.edad %>" required>
    </div>
    
    <div class="form-group">
        <label>Planeta de Origen</label>
        <input type="text" name="planetaOrigen" value="<%= heroe.planetaOrigen || 'Tierra' %>">
    </div>
    
    <div class="form-group">
        <label>Debilidad</label>
        <input type="text" name="debilidad" value="<%= heroe.debilidad || '' %>">
    </div>
    
    <div class="form-group">
        <label>Poderes (separados por coma)</label>
        <input type="text" name="poderes" value="<%= heroe.poderes ? heroe.poderes.join(', ') : '' %>">
    </div>
    
    <div class="form-group">
        <label>Aliados (separados por coma)</label>
        <input type="text" name="aliados" value="<%= heroe.aliados ? heroe.aliados.join(', ') : '' %>">
    </div>
    
    <div class="form-group">
        <label>Enemigos (separados por coma)</label>
        <input type="text" name="enemigos" value="<%= heroe.enemigos ? heroe.enemigos.join(', ') : '' %>">
    </div>
    
    <div class="form-actions">
        <button type="submit" class="btn save">💾 Actualizar</button>
        <a href="/dashboard" class="btn cancel">❌ Cancelar</a>
    </div>
</form>

<%- include('partials/footer') %>